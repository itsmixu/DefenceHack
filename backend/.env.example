# API keys for upstream data providers. Copy to .env and fill in.
# .env is gitignored; never commit real secrets.

MML_API_KEY=
FMI_API_KEY=
OPENCELLID_API_KEY=
N2YO_API_KEY=

# Optional overrides — used when an upstream renames its WFS layer or endpoint.
STATFIN_PAAVO_LAYER=
DIGIROAD_WFS_URL=
DIGIROAD_LAYER=
